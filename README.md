# Schield-CRYPT
The code provides an AES encryption and decryption tool with secure password handling, using PBKDF2 to derive the key. Users can encrypt or decrypt texts, following password complexity requirements. Encrypted data can be saved to a file, with error handling via logging.
